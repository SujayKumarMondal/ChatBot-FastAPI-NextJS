{
  "buildCommand": "cd fastapi_backend && python -m pip install --upgrade -qq pip setuptools wheel && pip install --no-cache-dir --only-binary :all: -r requirements.txt && alembic upgrade head",
  "startCommand": "cd fastapi_backend && gunicorn -w 4 -k uvicorn.workers.UvicornWorker --timeout 120 --bind 0.0.0.0:${PORT:-10000} fastapi_server:app",
  "envVars": [
    {
      "key": "PYTHON_VERSION",
      "value": "3.11.0"
    }
  ]
}
